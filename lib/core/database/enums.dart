enum DeviceType {
  pc,
  console,
}

enum DeviceStatus {
  idle,
  active,
  paused,
}

enum SessionMode {
  online,
  offline,
}

enum SessionStatus {
  active,
  paused,
  ended,
}

enum MenuCategory {
  food,
  drink,
}
